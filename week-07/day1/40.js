'use strict'

var aj = 'kuty'
// write a function that gets a string as an input
// and appends an 'a' character to its end and returns a new string

function add_a_to_string(string){
    var new_string = string + 'a';
    return new_string;
}

console.log(add_a_to_string(aj));
